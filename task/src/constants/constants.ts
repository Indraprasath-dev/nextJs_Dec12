// Base URL for API
export const API_BASE_URL = "https://dev-protocol-labs-network-api.herokuapp.com/v1/members";

// Default limit for pagination in API request
export const DEFAULT_LIMIT = 30;

// Default size for slicing
export const MAX_VISIBLE_SKILLS = 2;
